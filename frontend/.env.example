# Frontend API base for absolute API calls.
# Keep empty for local proxy-based development.
VITE_API_BASE=

# Vite dev proxy target.
# Local backend mode (default): http://localhost:5000
# Docker + nginx mode: http://localhost
VITE_PROXY_TARGET=http://localhost:5000
